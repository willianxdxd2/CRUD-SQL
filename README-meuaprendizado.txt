Exercicio de mySQL focado em CRUD(CREATE,READ,UPDATE,DELETE)
comandos usados
SELECT * FROM
DELETE
CREATE DATABASES
WHERE
CREATE TABLE
ADD COLUMN
UPDATE , SET
LIKE%
USE
CREATE DATA BASES
ALTER TABLE MODIFY COLUMN
INSERT INTO
DROP(para apagar bancos tabelas ou colunas)
obs: utilizar os comandos de mysql todos em caixa alta,sempre tento usar o terminal ao maximo e evitar usar a interface, e sempre atento a usar o select antes de usar o delete para não apagar ou sobrescrever o banco de dados todo, lembrar também de utilizar corretamente os tipos de dados, para manter o codigo organizado e não ocupar espaço desnecessario na memória.